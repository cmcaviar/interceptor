# Токен вашего Telegram бота (получить у @BotFather)
BOT_TOKEN=your_bot_token_here

# ID администраторов бота (через запятую)
# Эти пользователи смогут управлять настройками через команды бота
# Получить свой ID можно у @userinfobot
ADMIN_IDS=123456789,987654321

# ============================================================================
# НАСТРОЙКА БАЗЫ ДАННЫХ
# ============================================================================
#
# При использовании Docker Compose:
# DATABASE_URL автоматически устанавливается в docker-compose.yml
# Вам НЕ нужно указывать DATABASE_URL здесь при использовании Docker
#
# При запуске локально (без Docker):
# Раскомментируйте и настройте строку подключения к PostgreSQL
# DATABASE_URL=postgresql://bot_user:bot_password@localhost:5432/telegram_bot
# BOT_TOKEN=8048507069:AAHSL6prTuoLqMvjQGwCZOcLEVTxUQZYqoQ
# ============================================================================

# ============================================================================
# УПРАВЛЕНИЕ ДАННЫМИ
# ============================================================================
#
# Все настройки (топики, чаты, конфигурация) хранятся в PostgreSQL
#
# Docker Compose автоматически:
# 1. Запускает PostgreSQL контейнер
# 2. Создает базу данных telegram_bot
# 3. Инициализирует схему через yoyo-migrations при запуске бота
#
# После запуска `docker-compose up -d` добавьте данные одним из способов:
#
# Способ 1: Через админ-панель бота (рекомендуется)
#   Отправьте /start или /admin боту в Telegram
#
# Способ 2: Через SQL
#   docker-compose exec postgres psql -U bot_user -d telegram_bot
#
# Примеры SQL команд:
#
# Добавить топик:
#   INSERT INTO topics (prefix, name, topic_id) VALUES ('1', 'Sky', 289);
#
# Добавить исходный чат:
#   INSERT INTO source_chats (chat_id, name) VALUES ('-1001234567890', 'MyChat');
#
# Установить целевой чат:
#   UPDATE bot_config SET value = '-1003286030222' WHERE key = 'target_chat_id';
#
# Просмотреть данные:
#   SELECT * FROM topics;
#   SELECT * FROM source_chats;
#   SELECT * FROM bot_config;
#
# ============================================================================
