# Telegram Message Interceptor Bot

Telegram-бот для перехвата и перенаправления сообщений между чатами с маршрутизацией по префиксам в разные темы. Бот отслеживает сообщения, начинающиеся с `/`, в исходном чате и автоматически отправляет их в соответствующие темы целевого чата.

## Возможности

- Перехват сообщений, начинающихся с `/` в указанном чате или чатах
- Поддержка нескольких исходных чатов одновременно
- Маршрутизация сообщений в разные темы по префиксу (например, `/sky 27.5` или `/1 27.5` → тема "Скай")
- Поддержка как текстовых, так и цифровых префиксов
- Автоматический ответ со списком доступных тем при использовании неизвестного префикса
- Показ маппинга "цифра → название чата" при ошибке (если настроено)
- Автоматическое добавление информации об отправителе
- Гибкая настройка префиксов и тем через `.env` файл
- Логирование всех операций

## Требования

- Docker и Docker Compose
- Telegram Bot Token (получить у [@BotFather](https://t.me/botfather))
- Целевой чат должен быть Supergroup с включенными темами (Topics)
- Права администратора для бота в обоих чатах

## Быстрый старт с Docker

### 1. Клонируйте репозиторий

```bash
git clone <repository_url>
cd interceptor
```

### 2. Настройка конфигурации

Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

Отредактируйте `.env` файл:
```env
BOT_TOKEN=ваш_токен_бота_от_BotFather
ADMIN_IDS=ваш_telegram_user_id
```

Узнать свой Telegram User ID можно у бота [@userinfobot](https://t.me/userinfobot)

### 3. Запуск через Docker Compose

```bash
# Запустить все сервисы (PostgreSQL + бот)
docker-compose up -d

# Проверить логи
docker-compose logs -f
```

База данных PostgreSQL автоматически инициализируется при первом запуске бота с помощью **yoyo-migrations**.

### 4. Добавление данных через админ-панель

После запуска бота, отправьте ему команду `/start` или `/admin` в Telegram:

1. **Установите целевой чат:**
   - В меню выберите "🎯 Установить целевой чат"
   - Отправьте chat_id целевого чата (например: `-1003286030222`)

2. **Добавьте топики:**
   - Выберите "📋 Управление топиками" → "➕ Добавить топик"
   - Отправьте в формате: `1:Михаил:287`
   - Повторите для всех нужных топиков

3. **Добавьте исходные чаты:**
   - Выберите "💬 Управление исходными чатами" → "➕ Добавить чат"
   - Отправьте в формате: `-1003698440205:Trust`
   - Повторите для всех исходных чатов

**Альтернативно через SQL:**

```bash
docker-compose exec postgres psql -U bot_user -d telegram_bot

INSERT INTO topics (prefix, name, topic_id) VALUES ('1', 'Михаил', 287);
INSERT INTO source_chats (chat_id, name) VALUES ('-1003698440205', 'Trust');
UPDATE bot_config SET value = '-1003286030222' WHERE key = 'target_chat_id';
\q
```

### 5. Управление сервисами

```bash
# Остановить сервисы
docker-compose down

# Перезапустить бота (после изменений в коде)
docker-compose restart interceptor-bot

# Просмотр логов
docker-compose logs -f interceptor-bot

# Удалить все (включая данные в БД)
docker-compose down -v
```

## Установка без Docker (локально)

Если вы хотите запустить бота локально без Docker:

### 1. Установка PostgreSQL

**macOS (через Homebrew):**
```bash
brew install postgresql@15
brew services start postgresql@15
```

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
```

### 2. Создание базы данных

```bash
psql -U postgres
CREATE DATABASE telegram_bot;
CREATE USER bot_user WITH PASSWORD 'bot_password';
GRANT ALL PRIVILEGES ON DATABASE telegram_bot TO bot_user;
\q
```

### 3. Инициализация схемы

```bash
psql -U bot_user -d telegram_bot -f schema.sql
```

### 4. Установка зависимостей и запуск

```bash
pip install -r requirements.txt
cp .env.example .env
# Отредактируйте .env (добавьте BOT_TOKEN и ADMIN_IDS)
# Установите DATABASE_URL=postgresql://bot_user:bot_password@localhost:5432/telegram_bot
python bot.py
```

### 5. Добавление данных

Используйте команду `/start` или `/admin` в боте для добавления топиков и чатов через админ-панель.

## Получение Chat ID и Topic ID

### Получение Chat ID

Чтобы узнать ID чата, используйте вспомогательный скрипт:

1. Запустите скрипт:
```bash
python get_chat_id.py
```

2. Отправьте любое сообщение в чат с ботом (можно добавить бота в группу)

3. Скрипт выведет информацию о чате, включая Chat ID

4. Скопируйте Chat ID и добавьте его в `.env` файл

### Получение Topic ID (ID темы)

Чтобы узнать ID темы в Supergroup:

1. Запустите скрипт:
```bash
python get_topic_id.py
```

2. Отправьте сообщение **в нужную тему** целевого чата

3. Скрипт выведет Topic ID (message_thread_id) этой темы

4. Добавьте соответствие префикса и Topic ID в `.env` файл в параметр `TOPIC_ROUTING`

## Настройка бота в Telegram

1. Создайте бота через [@BotFather](https://t.me/botfather):
   - Отправьте команду `/newbot`
   - Следуйте инструкциям и получите токен

2. Добавьте бота в оба чата (исходный и целевой)

3. **Важно:** Дайте боту права администратора в обоих чатах или убедитесь, что у него есть права:
   - Отправлять сообщения
   - Читать сообщения (для группы: отключите "Privacy Mode" у @BotFather командой `/setprivacy`)

### Отключение Privacy Mode

По умолчанию боты в группах не видят все сообщения. Чтобы исправить это:

1. Напишите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/setprivacy`
3. Выберите вашего бота
4. Выберите `Disable`

## Запуск

Запустите бота командой:
```bash
python bot.py
```

Бот начнет работу и будет перехватывать сообщения из исходного чата, отправляя их в целевой чат.

## Управление через админ-панель бота

Бот имеет встроенную админ-панель для управления настройками прямо из Telegram.

### Настройка администраторов

В файле `.env` укажите ID пользователей-администраторов:

```env
ADMIN_IDS=123456789,987654321
```

Узнать свой ID можно у [@userinfobot](https://t.me/userinfobot)

### Команды админ-панели

**Главная команда:**
```
/admin - Открыть админ-панель
```

**Доступные функции:**

📋 **Управление топиками**
- Просмотр всех топиков
- Добавление нового топика (префикс:название:topic_id)
- Редактирование топика
- Удаление топика

💬 **Управление исходными чатами**
- Просмотр всех исходных чатов
- Добавление нового чата (chat_id:название)
- Включение/отключение чата
- Удаление чата

🎯 **Настройка целевого чата**
- Установка ID целевого чата для пересылки сообщений

📊 **Статистика**
- Количество топиков
- Количество активных исходных чатов
- Текущий целевой чат

### Примеры использования

1. **Добавить новый топик:**
   - Отправьте `/admin`
   - Выберите "📋 Управление топиками"
   - Нажмите "➕ Добавить топик"
   - Отправьте: `13:Айран:300`

2. **Добавить исходный чат:**
   - Отправьте `/admin`
   - Выберите "💬 Управление исходными чатами"
   - Нажмите "➕ Добавить чат"
   - Отправьте: `-1001234567890:Новый чат`

3. **Изменить целевой чат:**
   - Отправьте `/admin`
   - Выберите "🎯 Установить целевой чат"
   - Отправьте новый chat_id

## Управление через SQL (альтернатива)

Вы также можете управлять данными напрямую через SQL запросы.

### Просмотр данных

```sql
-- Все топики
SELECT * FROM topics ORDER BY prefix;

-- Все исходные чаты
SELECT * FROM source_chats;

-- Конфигурация бота
SELECT * FROM bot_config;
```

### Изменение данных

```sql
-- Обновить название топика
UPDATE topics SET name = 'Новое название' WHERE prefix = '1';

-- Обновить topic_id
UPDATE topics SET topic_id = 999 WHERE prefix = '1';

-- Деактивировать исходный чат
UPDATE source_chats SET is_active = FALSE WHERE chat_id = '-1001234567890';

-- Активировать исходный чат
UPDATE source_chats SET is_active = TRUE WHERE chat_id = '-1001234567890';

-- Удалить топик
DELETE FROM topics WHERE prefix = '1';
```

### Настройки в таблице bot_config

Доступные ключи конфигурации:

- `target_chat_id` - ID целевого чата для перенаправления
- `include_sender_info` - добавлять ли информацию об отправителе (`true`/`false`)
- `sender_format` - формат сообщения с отправителем
  - Доступные переменные: `{sender_name}`, `{sender_username}`, `{sender_id}`, `{message}`

```sql
-- Изменить формат сообщения
UPDATE bot_config
SET value = '{message}\n\nОт: {sender_name}'
WHERE key = 'sender_format';

-- Отключить информацию об отправителе
UPDATE bot_config
SET value = 'false'
WHERE key = 'include_sender_info';
```

## Пример работы

### Сценарий 1: Сообщение с известным префиксом

1. В `.env` настроена маршрутизация: `TOPIC_ROUTING=sky:123`
2. Пользователь Иван в исходном чате пишет: `/sky 27.5`
3. Бот перехватывает сообщение
4. Бот отправляет в тему с ID 123 целевого чата:
   ```
   27.5
   Отправил: Иван
   ```
5. В логах появляется: "Перенаправлено сообщение с префиксом 'sky' в тему 123: 27.5... (от Иван)"

### Сценарий 2: Несколько префиксов

Настройка: `TOPIC_ROUTING=sky:123,earth:456,ocean:789`

- `/sky данные` → отправится в тему 123
- `/earth информация` → отправится в тему 456
- `/ocean значение` → отправится в тему 789
- `/unknown текст` → бот ответит: "Такой темы нет, список доступных: /earth, /ocean, /sky"

### Сценарий 2.1: Цифровые префиксы с маппингом

Настройка:
```
TOPIC_ROUTING=1:123,2:456,3:789
TOPIC_NAMES=1:Sky,2:Earth,3:Ocean
```

- `/1 данные` → отправится в тему 123
- `/2 информация` → отправится в тему 456
- `/3 значение` → отправится в тему 789
- `/9 текст` → бот ответит:
  ```
  Такой темы нет, список доступных:
  /1 → Sky
  /2 → Earth
  /3 → Ocean
  ```

### Сценарий 3: Несколько исходных чатов

Настройка:
```
SOURCE_CHAT_IDS=-1001111111111,-1002222222222
TOPIC_ROUTING=sky:123,earth:456
```

Бот будет перехватывать сообщения с `/` из обоих чатов и отправлять их в соответствующие темы целевого чата. Это удобно для объединения сообщений из нескольких групп в один централизованный чат.

## Структура проекта

```
interceptor/
├── bot.py              # Основной файл бота
├── admin.py            # Админ-панель для управления через Telegram
├── database.py         # Модуль для работы с PostgreSQL
├── schema.sql          # SQL схема базы данных (автоинициализация в Docker)
├── config.py           # Конфигурация (legacy, можно удалить)
├── get_chat_id.py      # Вспомогательный скрипт для получения Chat ID
├── get_topic_id.py     # Вспомогательный скрипт для получения Topic ID
├── docker-compose.yml  # Docker Compose конфигурация
├── Dockerfile          # Dockerfile для бота
├── requirements.txt    # Зависимости Python
├── .env.example        # Пример файла конфигурации
├── .env                # Ваш файл конфигурации (создается вами)
├── .gitignore          # Игнорируемые файлы
└── README.md           # Документация
```

## Логирование

Бот выводит логи в консоль:
- Информация о запуске
- Перенаправленные сообщения
- Ошибки при отправке

## Решение проблем

### Ошибка подключения к базе данных
- Убедитесь, что PostgreSQL запущен
- Проверьте правильность `DATABASE_URL` в `.env`
- Проверьте, что база данных создана: `psql -U postgres -l`
- Проверьте права доступа пользователя к базе данных

### База данных не инициализирована
- Запустите `python init_db.py` для создания таблиц
- Проверьте наличие таблиц: `psql -U postgres -d telegram_bot -c "\dt"`

### Бот не видит топики из базы данных
- Проверьте, что данные есть в таблице: `SELECT * FROM topics;`
- Перезапустите бота, чтобы данные загрузились заново
- Проверьте логи бота при запуске

### Бот не видит сообщения в группе
- Убедитесь, что Privacy Mode отключен у @BotFather
- Проверьте, что бот добавлен в группу
- Убедитесь, что chat_id группы добавлен в таблицу `source_chats` и `is_active = TRUE`

### Бот не может отправлять сообщения
- Убедитесь, что бот имеет права администратора или права на отправку сообщений
- Проверьте правильность `target_chat_id` в таблице `bot_config`

### Неправильный Chat ID
- Используйте `get_chat_id.py` для получения правильного ID
- Для групп ID обычно отрицательный (например, `-1001234567890`)

### Бот не отправляет сообщения в тему
- Убедитесь, что целевой чат - это Supergroup с включенными темами
- Используйте `get_topic_id.py` для получения правильного Topic ID
- Проверьте правильность данных в таблице `topics`
- Убедитесь, что бот имеет права на отправку сообщений в темы

### Как включить темы в группе Telegram
1. Преобразуйте обычную группу в Supergroup (если еще не сделано)
2. Зайдите в настройки группы
3. Найдите пункт "Topics" или "Темы"
4. Включите темы
5. Создайте нужные темы

## Безопасность

- Не публикуйте файл `.env` с вашими токенами
- Храните BOT_TOKEN в секрете
- Регулярно проверяйте логи на предмет подозрительной активности

## Лицензия

MIT
